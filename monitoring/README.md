# Yanlord Booking 监控系统

## 📊 统一监控平台

所有监控服务已统一运行在 Kubernetes 集群中。

### 访问地址

- **Grafana**: http://localhost:30300 (用户名: `admin`, 密码: `admin`)
- **Prometheus**: http://localhost:30090
- **Loki**: 在集群内部运行 (通过 Grafana 访问)

### 可用的 Dashboard

1. **Kubernetes 集群监控** - 查看节点、Pod、容器的 CPU/内存使用情况
2. **Yanlord Booking 日志** - 查看应用日志、错误统计、业务指标

### 快速开始

所有服务已自动部署在 Kubernetes 中，无需额外配置。直接访问 Grafana 即可：

```bash
open http://localhost:30300
```

### 架构

- **Prometheus** - 采集 Kubernetes 集群和应用指标
- **Loki** - 聚合和查询日志
- **Grafana** - 统一可视化平台
- **kube-state-metrics** - 暴露 Kubernetes 资源状态

所有组件运行在 Kubernetes 中，通过 NodePort 暴露服务。
